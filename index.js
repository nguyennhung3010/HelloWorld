/** @format */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';

// import BasicFlatList from './component/BasicFlatList';
// AppRegistry.registerComponent(appName, () => BasicFlatList);

import App from './App';
AppRegistry.registerComponent(appName, () => App);

// import FetchExample from './server/FetchExample';
// AppRegistry.registerComponent(appName, () => FetchExample);
